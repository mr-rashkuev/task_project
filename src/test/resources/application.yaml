spring:
  liquibase:
    change-log: classpath:db/changelog/changelog-master.yaml
  datasource:
    url: jdbc:h2:mem:db
    driver-class-name: org.h2.Driver
    username: sa
    password: sa

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    open-in-view: false